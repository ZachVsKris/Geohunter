import { CATEGORIES } from "../../lib/categories";
import { DATASET_VERSION, RULES_VERSION } from "../../lib/version";
export default function DataPage(){return <main style={{maxWidth:1000,margin:"40px auto",padding:20}}><h1>Data & methodology</h1><p>Dataset release: {DATASET_VERSION}. Rules version: {RULES_VERSION}. Values come from World Bank World Development Indicators. Each category uses one documented indicator code, one direction, and one unit. Rounds are rejected when data is missing, implausible, too old, or too widely separated by year.</p><p><a href="/">Back to game</a> · <a href="/audit">Open audit page</a></p>{CATEGORIES.map(c=><section key={c.id} style={{padding:"14px 0",borderBottom:"1px solid #333"}}><h2>{c.icon} {c.name}</h2><p>{c.description}</p><code>{c.indicator}</code> · {c.direction === "high" ? "highest wins" : "lowest wins"} · {c.unit}<br/><a href={`https://data.worldbank.org/indicator/${c.indicator}`} target="_blank">Official World Bank source ↗</a></section>)}</main>}
