# Geo: Second Coming — v10.5.1 Stable Recovery

This build rolls the application back to the last confirmed working round-generation code from v10.5.

Safe changes only:

- Keeps the compact single-screen mobile drag-and-drop board
- Keeps the original v10.5 World Bank loading and round-generation path
- Fixes newly generated random seeds so they contain no hidden line breaks
- Copies a clean canonical challenge URL
- Shares the score and challenge URL as one message
- Simplifies and clarifies the player-facing rules
- Adds a compact 2 × 4 category layout on shorter desktop screens

No API routes, caching layer, alternate solver, or data-engine changes were added.
